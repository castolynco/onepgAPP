import 'package:flutter/material.dart';

const defaultIconSize = 32.0;
const smallWidth = SizedBox(width: 10.0);
const smallHeight = SizedBox(height: 15.0);
const largeHeight = SizedBox(height: 15.0);
const defaultInsidePading = 20.0;
const defaultInsidePading2 = 25.0;
const smallestHeight = SizedBox(
  height: 7.0,
);

const largeWidth = SizedBox(width: 40.0);
